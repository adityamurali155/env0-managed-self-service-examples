version: 1


deploy:
  steps:
    terraformInit:
      after:
        - terraform validate
    terraformApply:
      after:
        - terraform output -json > tf_output.json
        - pip install ansible
        - ansible-playbook attach_policy.yml